print("Hello! What's your name?");
string Name => input("name> ");
if (Name == "Anthony") then {
    print("Wait, what's your last name?");
    string L_Name => input("> ");
    if (L_Name == "Smith") then {
        print("OMG That's so cool! That's my name too!");
    } else {
        print("Oh, nevermind");
    };
} else {
    print("Oh, hello " + Name);
};